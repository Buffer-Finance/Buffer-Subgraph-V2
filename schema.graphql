
type BFRInvestor @entity {
  id: String!
  balance: BigInt!
  count: BigInt!
}

type BFRInvestorsCount @entity {
  id: String!
  holders: Int!
  timestamp: BigInt!
}

# swap from,to
# swap if from balance reached 0, cnt--  cnt++