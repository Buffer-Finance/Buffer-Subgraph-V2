
type BFRHolder @entity {
  id: String!
  balance: BigInt!
  count: BigInt!
}
type BFRInvestors @entity {
  id: String!
  count: BigInt!
}

# swap from,to
# swap if from balance reached 0, cnt--  cnt++